# CCJK Remote Control - Fast Track Plan (快速实施方案)

**Created**: 2026-01-19
**Goal**: 最简单、最快速的远程控制 Claude Code CLI
**Timeline**: 2-3 周完成核心功能

---

## 🎯 核心理念：极简主义

> **"最简单的方式，远程操作 Claude Code CLI"**

### 设计原则
1. ✅ **零配置** - 一条命令启动
2. ✅ **零学习** - 发邮件就能用
3. ✅ **零依赖** - 不需要额外服务
4. ✅ **零成本** - 完全免费（初期）

---

## 🚀 最简方案：Email-to-CLI Bridge

### 为什么选择邮件？

| 方案 | 复杂度 | 开发时间 | 用户体验 | 成本 |
|------|--------|----------|----------|------|
| WebSocket + 云服务 | ⭐⭐⭐⭐⭐ | 16-20周 | ⭐⭐⭐⭐⭐ | $500+/月 |
| Web Dashboard | ⭐⭐⭐⭐ | 10-14周 | ⭐⭐⭐⭐ | $200+/月 |
| **Email Bridge** | ⭐⭐ | **2-3周** | ⭐⭐⭐⭐ | **$0-50/月** |
| Telegram Bot | ⭐⭐⭐ | 4-6周 | ⭐⭐⭐ | $0 |

**邮件方案优势**：
- ✅ 人人都会用（发邮件）
- ✅ 不需要安装客户端
- ✅ 手机、电脑都能用
- ✅ 异步工作流（发完就走）
- ✅ 开发最快（2-3周）

---

## 📐 架构设计：三层结构

```
┌─────────────────────────────────────────────────────────────────┐
│                    极简远程控制架构                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📱 你的设备（任何地方）                                          │
│       │                                                          │
│       │ 1. 发邮件到: task@your-project.com                       │
│       │    主题: "运行测试"                                       │
│       │    内容: "npm test"                                      │
│       │                                                          │
│       ▼                                                          │
│  ☁️ 邮件网关（免费服务）                                          │
│       │                                                          │
│       ├─ Cloudflare Email Routing (免费)                         │
│       ├─ 或 Mailgun (免费额度: 5000封/月)                        │
│       └─ 或 SendGrid (免费额度: 100封/天)                        │
│       │                                                          │
│       │ 2. 转发到你的邮箱: your-email+ccjk@gmail.com             │
│       │                                                          │
│       ▼                                                          │
│  💻 本地 CCJK Agent（后台运行）                                   │
│       │                                                          │
│       ├─ 每30秒检查邮箱（IMAP）                                   │
│       ├─ 解析邮件命令                                             │
│       ├─ 执行 Claude Code CLI                                    │
│       └─ 发送结果邮件                                             │
│       │                                                          │
│       ▼                                                          │
│  📂 你的项目 + Claude Code                                        │
│       │                                                          │
│       ▼                                                          │
│  📧 你收到结果邮件                                                │
│       │                                                          │
│       │ 主题: "✅ 任务完成: 运行测试"                             │
│       │ 内容: "所有测试通过 ✓"                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 技术实现：三个核心模块

### 模块 1: CCJK Daemon（后台守护进程）

**文件**: `src/daemon/index.ts`

```typescript
// 核心功能
export class CcjkDaemon {
  private emailChecker: EmailChecker
  private taskExecutor: TaskExecutor
  private resultSender: ResultSender

  async start() {
    console.log('🚀 CCJK Daemon started')

    // 每30秒检查一次邮件
    setInterval(async () => {
      await this.checkAndExecute()
    }, 30000)
  }

  private async checkAndExecute() {
    // 1. 检查新邮件
    const emails = await this.emailChecker.fetchNew()

    // 2. 解析命令
    for (const email of emails) {
      const task = this.parseEmail(email)

      // 3. 执行命令
      const result = await this.taskExecutor.run(task)

      // 4. 发送结果
      await this.resultSender.send(email.from, result)
    }
  }
}
```

**CLI 命令**:
```bash
ccjk daemon start    # 启动后台服务
ccjk daemon stop     # 停止服务
ccjk daemon status   # 查看状态
ccjk daemon logs     # 查看日志
```

---

### 模块 2: Email Checker（邮件检查器）

**文件**: `src/daemon/email-checker.ts`

```typescript
import Imap from 'imap'
import { simpleParser } from 'mailparser'

export class EmailChecker {
  private imap: Imap

  constructor(config: EmailConfig) {
    this.imap = new Imap({
      user: config.email,
      password: config.password,
      host: 'imap.gmail.com',
      port: 993,
      tls: true
    })
  }

  async fetchNew(): Promise<Email[]> {
    return new Promise((resolve) => {
      this.imap.connect()

      this.imap.once('ready', () => {
        this.imap.openBox('INBOX', false, () => {
          // 搜索未读邮件，主题包含 [CCJK]
          this.imap.search(['UNSEEN', ['SUBJECT', '[CCJK]']], (err, results) => {
            if (results.length === 0) {
              resolve([])
              return
            }

            const fetch = this.imap.fetch(results, { bodies: '' })
            const emails: Email[] = []

            fetch.on('message', (msg) => {
              msg.on('body', (stream) => {
                simpleParser(stream, (err, parsed) => {
                  emails.push({
                    from: parsed.from.text,
                    subject: parsed.subject,
                    body: parsed.text,
                    date: parsed.date
                  })
                })
              })
            })

            fetch.once('end', () => {
              resolve(emails)
              this.imap.end()
            })
          })
        })
      })
    })
  }
}
```

---

### 模块 3: Task Executor（任务执行器）

**文件**: `src/daemon/task-executor.ts`

```typescript
import { exec } from 'tinyexec'

export class TaskExecutor {
  async run(task: Task): Promise<TaskResult> {
    try {
      console.log(`🔄 Executing: ${task.command}`)

      // 执行 Claude Code CLI
      const result = await exec('claude', task.command.split(' '), {
        cwd: task.projectPath,
        timeout: 300000 // 5分钟超时
      })

      return {
        success: true,
        output: result.stdout,
        error: null,
        duration: Date.now() - task.startTime
      }
    } catch (error) {
      return {
        success: false,
        output: null,
        error: error.message,
        duration: Date.now() - task.startTime
      }
    }
  }
}
```

---

## 📧 邮件命令格式

### 基础格式

```
收件人: task@your-project.com
主题: [CCJK] 你的任务描述
正文: 具体命令
```

### 示例 1: 运行测试

```
收件人: task@your-project.com
主题: [CCJK] 运行测试
正文: npm test
```

### 示例 2: Git 提交

```
收件人: task@your-project.com
主题: [CCJK] 提交代码
正文: git add . && git commit -m "fix: bug修复"
```

### 示例 3: Claude Code 对话

```
收件人: task@your-project.com
主题: [CCJK] 代码审查
正文: 请审查 src/utils/config.ts 文件，找出潜在问题
```

### 示例 4: 查询状态

```
收件人: task@your-project.com
主题: [CCJK] 状态查询
正文: status
```

---

## 🔐 安全设计

### 1. 邮件白名单

```typescript
// src/daemon/security.ts
export class SecurityManager {
  private allowedEmails = [
    'your-email@gmail.com',
    'team-member@company.com'
  ]

  isAllowed(email: string): boolean {
    return this.allowedEmails.includes(email)
  }
}
```

### 2. 命令白名单

```typescript
// 只允许安全命令
const ALLOWED_COMMANDS = [
  'npm test',
  'npm run build',
  'git status',
  'git log',
  'claude', // Claude Code CLI
  'status'  // 查询状态
]

// 危险命令黑名单
const BLOCKED_COMMANDS = [
  'rm -rf',
  'sudo',
  'chmod',
  'git push --force'
]
```

### 3. 沙箱执行

```typescript
// 在隔离环境中执行
const result = await exec(command, [], {
  cwd: projectPath,
  timeout: 300000,
  env: {
    ...process.env,
    NODE_ENV: 'production'
  }
})
```

---

## 📱 用户体验流程

### 首次设置（5分钟）

```bash
# 1. 安装 CCJK（如果还没装）
npx ccjk init

# 2. 配置邮件
ccjk daemon setup

# 提示输入：
# - 你的邮箱: your-email@gmail.com
# - 邮箱密码（应用专用密码）: ****
# - 允许的发件人: your-email@gmail.com

# 3. 启动守护进程
ccjk daemon start

# ✅ 完成！现在可以发邮件控制了
```

### 日常使用（10秒）

```
1. 打开邮件客户端（手机/电脑）
2. 新建邮件
3. 收件人: task@your-project.com
4. 主题: [CCJK] 运行测试
5. 正文: npm test
6. 发送 ✉️

---

等待 30-60 秒...

---

7. 收到回复邮件 📬
   主题: ✅ 任务完成: 运行测试
   内容:
   所有测试通过 ✓
   - 32 个测试
   - 耗时: 2.3秒
```

---

## 🗓️ 开发计划：2-3周完成

### Week 1: 核心功能（5天）

**Day 1-2: Daemon 基础架构**
- [ ] 创建 `src/daemon/` 目录结构
- [ ] 实现 `CcjkDaemon` 主类
- [ ] 实现进程管理（start/stop/status）
- [ ] 添加日志系统

**Day 3-4: Email Checker**
- [ ] 集成 `imap` 库
- [ ] 实现邮件检查逻辑
- [ ] 实现邮件解析
- [ ] 添加错误处理

**Day 5: Task Executor**
- [ ] 实现命令执行器
- [ ] 添加超时控制
- [ ] 添加输出捕获
- [ ] 测试基础功能

---

### Week 2: 安全与优化（5天）

**Day 6-7: 安全机制**
- [ ] 实现邮件白名单
- [ ] 实现命令白名单/黑名单
- [ ] 添加沙箱执行
- [ ] 添加速率限制

**Day 8-9: 结果发送**
- [ ] 集成 `nodemailer`
- [ ] 实现结果邮件发送
- [ ] 设计邮件模板
- [ ] 添加附件支持（日志文件）

**Day 10: 用户体验**
- [ ] 实现 `ccjk daemon setup` 向导
- [ ] 添加配置文件管理
- [ ] 优化错误提示
- [ ] 编写用户文档

---

### Week 3: 测试与发布（5天）

**Day 11-12: 测试**
- [ ] 单元测试（80%+ 覆盖率）
- [ ] 集成测试
- [ ] 真实环境测试
- [ ] 性能测试

**Day 13-14: 文档与示例**
- [ ] 编写 README
- [ ] 创建示例项目
- [ ] 录制演示视频
- [ ] 编写故障排除指南

**Day 15: 发布**
- [ ] 版本号: v3.7.0
- [ ] 发布到 npm
- [ ] 发布公告
- [ ] 收集用户反馈

---

## 📦 依赖包

```json
{
  "dependencies": {
    "imap": "^0.8.19",           // IMAP 邮件检查
    "mailparser": "^3.6.5",      // 邮件解析
    "nodemailer": "^6.9.7",      // 发送邮件
    "pm2": "^5.3.0",             // 进程管理（可选）
    "dotenv": "^16.3.1"          // 环境变量
  }
}
```

---

## 🎯 MVP 功能清单

### 必须有（Week 1-2）
- [x] 后台守护进程
- [x] 邮件检查（IMAP）
- [x] 命令执行
- [x] 结果发送
- [x] 基础安全（白名单）

### 应该有（Week 3）
- [ ] 配置向导
- [ ] 日志系统
- [ ] 错误处理
- [ ] 用户文档

### 可以有（未来版本）
- [ ] Web 控制面板
- [ ] Telegram Bot
- [ ] 多项目支持
- [ ] 任务队列
- [ ] 定时任务

---

## 🚀 快速启动指南

### 开发者（现在就开始）

```bash
# 1. 创建分支
git checkout -b feature/remote-control

# 2. 创建目录结构
mkdir -p src/daemon
mkdir -p src/daemon/types

# 3. 安装依赖
pnpm add imap mailparser nodemailer
pnpm add -D @types/imap @types/mailparser @types/nodemailer

# 4. 开始编码
code src/daemon/index.ts
```

### 用户（3周后）

```bash
# 1. 更新 CCJK
npm install -g ccjk@latest

# 2. 配置邮件
ccjk daemon setup

# 3. 启动服务
ccjk daemon start

# 4. 发邮件测试
# 收件人: task@your-project.com
# 主题: [CCJK] 测试
# 正文: echo "Hello CCJK!"

# 5. 等待回复（30-60秒）
```

---

## 💡 进阶功能（未来）

### 1. 智能命令解析

```
用户发送: "帮我运行测试"
AI 解析: npm test

用户发送: "提交代码，消息是修复bug"
AI 解析: git add . && git commit -m "fix: 修复bug"
```

### 2. 多项目支持

```
用户发送:
项目: ccjk
命令: npm test

---

系统自动切换到 ccjk 项目目录执行
```

### 3. 定时任务

```
用户发送:
命令: npm test
定时: 每天 9:00

---

系统每天 9:00 自动执行测试
```

### 4. 任务队列

```
用户连续发送 3 个任务：
1. npm test
2. npm run build
3. git push

---

系统按顺序执行，每个完成后发邮件通知
```

---

## 🎯 成功指标

### 技术指标
- ✅ 邮件响应时间 < 60秒
- ✅ 命令执行成功率 > 95%
- ✅ 系统稳定性 > 99%
- ✅ 测试覆盖率 > 80%

### 用户指标
- ✅ 首次设置时间 < 5分钟
- ✅ 日常使用时间 < 10秒
- ✅ 用户满意度 > 4.5/5
- ✅ 活跃用户 > 100人（3个月内）

---

## 🤔 常见问题

### Q1: 邮件会不会被拦截？
**A**: 使用 Gmail 应用专用密码，不会被拦截。建议使用 `+ccjk` 标签（如 `your-email+ccjk@gmail.com`）方便过滤。

### Q2: 安全吗？
**A**:
- ✅ 邮件白名单（只允许你的邮箱）
- ✅ 命令白名单（只允许安全命令）
- ✅ 沙箱执行（隔离环境）
- ✅ 本地运行（不经过第三方服务器）

### Q3: 需要一直开着电脑吗？
**A**: 是的，守护进程需要在本地运行。未来可以考虑：
- 云服务器部署（24小时运行）
- Docker 容器（轻量级）
- Raspberry Pi（低功耗）

### Q4: 支持多人协作吗？
**A**: 当前版本支持多个邮箱白名单。未来版本会添加：
- 权限管理
- 任务分配
- 审批流程

---

## 🎉 总结

### 为什么这个方案最好？

| 特性 | 评分 | 说明 |
|------|------|------|
| **简单性** | ⭐⭐⭐⭐⭐ | 发邮件就能用 |
| **速度** | ⭐⭐⭐⭐⭐ | 2-3周完成 |
| **成本** | ⭐⭐⭐⭐⭐ | 几乎免费 |
| **实用性** | ⭐⭐⭐⭐ | 满足80%需求 |
| **扩展性** | ⭐⭐⭐⭐ | 可升级到Web/App |

### 下一步行动

**立即开始**:
```bash
# 1. 创建分支
git checkout -b feature/remote-control

# 2. 开始编码
mkdir -p src/daemon
code src/daemon/index.ts

# 3. 3周后发布 v3.7.0
```

---

**让我们开始吧！🚀**
