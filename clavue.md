# clavue.md

Clavue is the primary development tool for this repository. This file is the operating baseline that Clavue's `/init` regenerates from local signals.

> Durable architecture, build commands, and gotchas live in [`docs/CLAUDE-NOTES.md`](./docs/CLAUDE-NOTES.md). Read that first — this file only carries the auto-detected baseline.

@./docs/CLAUDE-NOTES.md

## Regeneration Contract
- Running Clavue `/init` rewrites the auto-detected block below and the stubs in `CLAUDE.md` / `AGENTS.md`. Keep durable hand-authored guidance in `docs/CLAUDE-NOTES.md`, not here.
- After `/init` regenerates this file, restore the `@./docs/CLAUDE-NOTES.md` reference if it was removed.

<!-- CLAVUE_INIT_DETECTED_CONTEXT_START -->
<!-- This section is regenerated by /init from local repository signals. -->
## Repository
- Root: ccjk-public
- Top-level directories: assets, bin, dist, docs, evals, examples, html, packages

## Commands
- pnpm dev
- pnpm build
- pnpm start
- pnpm typecheck
- pnpm lint
- pnpm test:run
- pnpm format

## Detected Project Context
- package.json: package ccjk; type module; packageManager pnpm@10.17.1; scripts: pnpm dev (tsx ./src/cli.ts); pnpm build (pnpm build:release:deps && pnpm build:root); pnpm start (node bin/ccjk.mjs); pnpm typecheck (pnpm build:release:deps && pnpm typecheck:root); pnpm lint (eslint); pnpm test:run (vitest run); pnpm format (prettier --write src/**/*.ts); notable deps: eslint, typescript, vitest; uses workspaces
- pnpm-workspace.yaml: workspace packages include ., docs, and packages/*; uses catalog dependencies
- README.md: # 🚀 CCJK
- tsconfig.json: TypeScript project configuration present
- .prettierrc: Prettier configuration present
- docs: nested project candidate with package.json, README.md
<!-- CLAVUE_INIT_DETECTED_CONTEXT_END -->

## Default Development Protocol
- Inspect relevant files before editing; do not rely on stale assumptions.
- Prefer small, reversible changes and preserve unrelated user work.
- Run the smallest relevant verification command after edits; run the full gate before releases.
- Keep generated or packaged artifacts aligned with source when this repository tracks them.

## Handoff Notes
- Summarize changed behavior, verification commands, and remaining risks when finishing work.
- If detected commands or context are incomplete, update durable guidance sources and rerun `/init`.
