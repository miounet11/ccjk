# Pre-Release Postmortem Check
# 在发布前检查代码是否可能触发已知的 Postmortem 问题
#
# 使用方法:
# 1. 将此文件复制到你的项目 .github/workflows/ 目录
# 2. 确保项目已安装 ccjk: npm install -g ccjk
# 3. 运行 ccjk postmortem init 初始化 Postmortem 系统

name: Pre-Release Postmortem Check

on:
  pull_request:
    branches:
      - main
      - master
      - 'release/*'
  push:
    branches:
      - main
      - master

jobs:
  postmortem-check:
    name: 🔬 Postmortem Check
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 📥 Install dependencies
        run: pnpm install

      - name: 🔬 Run Postmortem Check
        run: |
          # 检查是否存在 postmortem 目录
          if [ -d "./postmortem" ]; then
            echo "📋 Found postmortem directory, running check..."
            npx ccjk postmortem check --ci
          else
            echo "⚠️ No postmortem directory found, skipping check"
            echo "💡 Run 'ccjk postmortem init' to initialize the postmortem system"
          fi

      - name: 📊 Upload Check Report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: postmortem-check-report
          path: ./postmortem/
          retention-days: 7

  # 可选：在 PR 中添加评论
  comment-on-pr:
    name: 💬 Comment on PR
    runs-on: ubuntu-latest
    needs: postmortem-check
    if: failure() && github.event_name == 'pull_request'

    steps:
      - name: 💬 Add PR Comment
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🔬 Postmortem Check Failed

            代码检查发现可能触发已知问题的代码模式。

            请查看 [检查详情](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) 并修复相关问题。

            ### 如何修复
            1. 查看 \`./postmortem/\` 目录中的相关报告
            2. 根据报告中的建议修改代码
            3. 重新提交

            ---
            *由 CCJK Postmortem System 自动生成*`
            })
